# 夜間作業まとめ（2026-07-13 早朝）

寝ている間に「テストで検証できる安全な範囲だけ」進めました。**すべて挙動不変**（本番の見た目・動作は変えていません）。
全作業は「テストが緑のときだけコミット」。**push は未実施**なので、朝あなたが GitHub Desktop で push してください。

---

## まず朝にやること（3分）

1. **push**（未pushのコミット11本）→ push すると自動で「テスト→ビルド→公開」が走ります（前夜に本番デプロイ自動化を設定済み）。
2. **Actions が緑**になったら **https://km4145-ship-it.github.io/study-app/** を Cmd+Shift+R で開いて確認。
3. 特に下記「要ブラウザ確認」を触ってみてください。

### 要ブラウザ確認（自動テストでは全経路を見きれない部分）
- 🔊 **読み上げ（TTS）**：問題文の音声。→ 日本語読み変換を別ファイルへ分離したため。
- 📝 **模試（試験）と RPG の経験値/レベル**：偏差値・シード出題・レベルアップ。→ 採点/進行の関数を別ファイルへ分離したため。
- 🃏 **フラッシュカード / 学習ノート / 記述問題**、🏅 **クエスト/バッジ**、🎙️ **声の設定画面**。→ これらのデータを別ファイルへ分離したため。
- ※ いずれも「同じコードを読み込む場所を変えただけ」で、分離前後で挙動が一致することは検証済みです。

### もし本番が変になったら（即ロールバック）
GitHub → Settings → Pages → Source を **「Deploy from a branch（main / root）」に戻す**だけで、元の動くアプリに即復帰します（データにも影響なし）。

---

## やったこと（11コミット）

### 1. 基盤の堅牢化（これが一番の成果・恒久的な安全網）
- **回帰ガード `scripts/check-globals.js`**：ビルドで圧縮した後も、`onclick` 等から呼ぶ
  **グローバル関数107個が全部生き残っているか**を機械チェック。
  「テストは緑なのに本番だけボタンが無反応」という最悪の事故を止める命綱。CI とデプロイ前に自動実行。
- **CI にビルド検証ジョブ**を追加（本番と同じ手順でビルド→健全性チェック）。
- **読み込み順の静的テスト**（依存順を崩す事故を防ぐ）。
- Node 20→24（GitHub の非推奨警告を解消）。

### 2. 設計ドキュメント `docs/ARCHITECTURE.md`
- 全体像・モジュール読み込み順・**データ同期モデル（過去のデータ消失対策の層）**・
  ビルド/デプロイ・テスト・**安全な変更手順**・スケール方針・チェックリストを1枚に集約。
- 将来の自分／他の人／AIが安全に手を入れるための地図。

### 3. モジュール分割（index.html を軽く：5685 → 5467 行）
すべて「分離前の実装からゴールデン値（出力/件数）を捕捉 → 分離後も一致」を確認済み。

| 新モジュール | 中身 | 安全性の担保 |
|--------------|------|--------------|
| `js/reading-ja.js` | 日本語の読み変換（TTS誤読対策） | ゴールデン19ケース一致 |
| `js/content-data.js` | フラッシュカード/学習ノート/記述問題データ | スナップショット完全一致 |
| `js/scoring.js` | 偏差値・試験シードRNG・RPG経験値 | ゴールデン18ケース一致 |
| `js/ui-data.js` | キャラ紹介/クエスト/バッジ/声カタログ | 件数・キー一致 |

### 現在の状態
- テスト **30本すべて緑**（分離前は24本）。ビルドOK・dist健全性OK。
- js/ モジュール **15個**（分離前11個）。index.html **5467行**（-218）。
- 作業ツリーはクリーン（全部コミット済み）。

---

## 次にやるとよいこと（起きたら相談で）
- **残りの安全なデータ分割**：MU_CHARS / SRS_INT / EXAM_QUOTA / RPG定数 / 生成用の要素配列（JP_ELEM_READ 等）。
- **純ヘルパの分離**：escapeHtml / 日付系(todayKey,fmtTime) / lsGetJSON など（特性テストを付けてから）。
- **中期（要塞くずし）**：`currentLevel…currentChar` の可変グローバル15個を `js/state.js` に集約 →
  クイズ/RPG の巨大UIを状態越しに切り出す。ここは**ブラウザ確認前提**で慎重に。
- 詳細は `docs/ARCHITECTURE.md` の §4（残っているもの）・§8（安全な手順）・§9（スケール方針）参照。
