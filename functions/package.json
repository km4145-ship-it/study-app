{
  "name": "study-app-functions",
  "private": true,
  "description": "ElevenLabs TTSキーのサーバー側プロキシ（docs/APP_STORE_ROADMAP.md Phase 2）",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
