# 作業まとめ（2026-07-19・第2波＋再監査＋実行）

おかえりなさい。「第2波の残りを終わらせ、その後もう一度 徹底レビューして次の改善点を洗い出し、実行まで」の依頼を、
**すべてローカルコミット（未push）・全72テスト合格を維持**して進めました。実機確認のうえ、あなたのタイミングで push してください。

このセッションで **11コミット**（第2波3件＋再監査＋監査由来の実装8件）。すべて
「純ロジックはテスト＋実ブラウザ(headless Chrome)で挙動確認 → 小刻みにコミット」で進めています。

---

## 1. まず第2波（前半セッションの続き）
| # | 実装 | 効果 |
|---|---|---|
| ① `231609d` | **習熟マップを4段階化**（▶練習中→○なじみ→◎とくい→👑マスター） | 正答率＋回数で「1回100%を"得意"にしない」教育的に正しい見立て。記録画面に習得率バー＋つぎはここ |
| ② `d10f04f` | **まちがい解き直しを強化** | 核（解説/類題リベンジ/ミス型/SRS）は既存だったので、唯一の弱点＝解説とリベンジの分断を`revTip`で接続（さっきの考え方をリベンジに運ぶ） |
| ③ `3292857` | **診断プレイスメント** | 全員 rating=50 のコールドスタートを解消。初回に短い適応診断（10問）で教科ごとの開始難易度を最適化。新規はオンボ後に自動起動＋記録ハブから再受験 |

## 2. もう一度 徹底レビュー（並列エージェント3体：学習科学／UX／技術）
実コードを読ませて根拠付きで次の改善点を洗い出し。**両監査が「診断プレイスメント」を最優先に挙げ**、方向性を裏取り。
そこから**効果×低リスク**の高い順に実行しました（↓）。

## 3. 監査由来の実装（8件）
**学習効果**
- `c935a24` **SRSを簡易HLR化**：反応速度×誤答回数で復習間隔を伸縮（速い正解は延ばし、遅い/難項目は縮める）。データは取得済みで未活用だった
- `05a261d` **リアルタイム難易度**：直近正答率(recentAccuracy)で「次の1問だけ」難易度を寄せてフロー帯(75-85%)を維持。単一教科ジェネ練習のみ・リベンジ尊重・模試/バトル等は除外
- `ecb264f` **単元インターリービング**：同一単元の連打（ブロック練習）→単元を混ぜて転移・定着を上げる。本文つき（読解）はガード

**継続・離脱防止（ダークパターンなし）**
- `2fefb38` **ログイン連続に欠席救済（フリーズ）**：1日欠席で7/14/…/365日の大型報酬が消える不整合を是正。「おやすみ券で守ったよ」の前向き通知＝休める設計
- `9f24e0e` **復帰導線**：数日ぶりの復帰を「おかえり、軽く3問から」で前向きに迎える（通知なし・アプリ内完結）
- `0d1cd3b` **夜のLINE再訪ナッジ**：`scripts/line-nudge.js`（**dry-run既定・実送信は--send時のみ・自動導入なし**）＋`docs/LINE_NUDGE_SETUP.md`。有効化はあなた判断

**技術・堅牢性**
- `40d3115` **CIで実ブラウザe2eを必須化**：従来 Chrome不在だと「黙ってスキップ→CI緑」で起動未検証だった穴を是正（ci.ymlにChrome導入＋E2E_REQUIRED=1）。deploy.ymlは非対象＝デプロイは妨げない
- `f678036` **char3d MutationObserver を rAF デバウンス**：毎mutationの全文書走査を1フレーム1回へ（挙動不変の性能改善）

---

## 4. 実機で見てほしいところ
- 記録📊 → **単元マスター度マップ**が4段階＋習得率バー＋「🎯つぎはここ」に
- 記録📊ハブ or 新規ユーザーのオンボ後 → **🎯じつりょくしんだん**（10問で開始難易度が決まる）
- 練習で連続正解 → **次の問題がスッと難しくなる**（低調なら易しくなる）／同じ単元が連続しにくい
- まちがえた直後のリベンジ問題に **「💡さっきの考え方：◯◯」**
- 1日空けてもログイン連続が消えない（**おやすみ券**）／数日ぶりに開くと **おかえりトースト**

## 5. 確認・デプロイ
```
cd ~/Documents/GitHub/study-app
node tests/run-all.js                              # 全72スイート合格
npm run build && node scripts/check-globals.js     # 本番同等ビルド＋健全性
```
問題なければ push → GitHub Actions が テスト→ビルド→公開。
※ 今回 CI に Chrome 導入＋e2e必須化を入れたので、**初回の push で CI が実ブラウザ起動まで検証**します
（もし CI 側の e2e が不安定なら、ci.yml の該当ステップを一時 `continue-on-error` にできます。デプロイ自体は従来どおり動きます）。

## 5.5 「再開」後の追加実装（+3コミット）
- `4e8d4cd` **「きょうやること」統一チェックリスト**（UX#2）：ハブ最上部に 学習/復習/タクトdaily/家族もんだい(+無料ガチャ) を ✅/⬜ の1リストへ集約。「何をやれば今日おわりか」を明確化（散在6系統の解消）。全部⬜が消えると「🎉ぜんぶできた！」
- `82b5581` **オンボに名前＋キャラ選択**（UX#3）：「ユーザー1」始まりを解消。学年の次に 名前入力＋12キャラのグリッド。実Chromeで「そら＋fox→ヘッダー反映」確認
- `73d05d3` **習熟マップをRPG大陸マップへ重畳**：大陸カードに「🗺️習得 XX%」バッジ（記録画面の`masterySummary`を再利用）

## 6. まだ残っている次の候補（未着手・PRO_ROADMAP.md 参照）
- **自信度申告**（回答前に かんぺき/たぶん/わからない → 過信ミスの可視化・メタ認知）[中/S]
  ※ 毎問だと友好で friction。実装するなら「たまに/任意」にするか要判断——ここは方針を決めたいので保留にしました
- **保証つき初日ウェルカムクエスト**（初日に必ず1報酬）[中/M]（※名前+キャラ＋診断は実装済）
- 技術の大物（**three.js(714KB)遅延ロード**／機能別コード分割）＝効果大だが破壊的。CIのe2e必須化（今回導入済）で守った上で、実機確認しながら慎重に [高/M〜L]
- 空catchの観測性回復（dev時のみエラーモニタへ流す）[中/M]

—— どれも「効果×実装容易性」で並べてあります。次回「再開」と言えば、ここの上から進めます。
